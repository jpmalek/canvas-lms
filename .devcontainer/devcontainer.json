{
    "name": "Canvas LMS Development",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "settings": { 
      "terminal.integrated.shell.linux": "/bin/bash"
    },
    "forwardPorts": [3000, 4000], // Example of ports Canvas LMS might use
    "extensions": [
      // List any VS Code extensions you want pre-installed
    ]
  }